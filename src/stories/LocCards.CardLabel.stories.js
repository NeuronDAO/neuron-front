import React from "react";

import CardLabel from "../components/LocCards/CardLabel";

export default {
  title: "Example/CardLabel",
  component: CardLabel,
};

const Template = (args) => <CardLabel {...args} />;

export const Default = Template.bind({});
