import React from "react";

import CardSlider from "../../components/renft/CardSlider.jsx";

export default {
  title: "Renft / Card Slider",
  component: CardSlider,
};

const Template = (args) => <CardSlider {...args} />;

export const Slider = Template.bind({});
