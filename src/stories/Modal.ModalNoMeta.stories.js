import React from "react";

import ModalNoMeta from "../components/Modal/ModalNoMeta";

export default {
  title: "Example/ModalNoMeta",
  component: ModalNoMeta,
};

const Template = (args) => <ModalNoMeta {...args} />;

export const Default = Template.bind({});
